apiVersion: v1
kind: ConfigMap
metadata:
  name:  {{ include "rasa-bot.fullname" . }}
data:
  endpoints.yml: |
{{- with .Values.rasa.endpoints }}
  {{- toYaml . | nindent 4 }}
{{- end }}
  credentials.yml: |
{{- with .Values.rasa.credentials }}
  {{- toYaml . | nindent 4 }}
{{- end }}
